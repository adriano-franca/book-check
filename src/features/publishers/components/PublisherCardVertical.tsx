// import type { Publisher } from "@/@types/publishers";

// interface PublisherCardVerticalProps {
//   publisher: Publisher;
//   onClick: () => void;
// }

// export const PublisherCardVertical = ({ publisher, onClick }: PublisherCardVerticalProps) => (
//   <div onClick={onClick} className="cursor-pointer">
//     <img src={publisher.logo} alt={publisher.name} className="w-full rounded" />
//     <p>{publisher.name}</p>
//   </div>
// );